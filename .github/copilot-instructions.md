# Copilot Instructions

## Project Guidelines
- Keep API logic simple and avoid making JSON rules dependent on API-side data models; keep lists defined in JSON and only use utilities to reduce repeated JSON rule code.